@model Ead_Project.Models.MoviePartial

@{
    ViewBag.Title = "DeleteMovie";
    Layout = "~/Views/Shared/AdminShared.cshtml";
}

<div class="container">
    <div class="row">
        <div class="col-md-3"></div>
        <div class="col-md-6">
            <div class="well well-sm" style="margin-top:40px">
               @using (Html.BeginForm("DeleteMovie1", "Admin", FormMethod.Post, new { @class = "form login-form" }))
               {
                <legend class="text-center header" style="color:green">Delete Movie</legend>
                @Html.TextBoxFor(x => x.Name, new { @class = "input-has-icon left-icon form-control" , placeholder = "Enter Movie Name" })
                <br/>
                <input type="submit" value="Delete Movie" class="btn btn-lg btn-success btn-block" style="color:white">
               }
     
                <!--<form action="/Admin/DeleteMovie1" method="post" enctype="multipart/form-data" class="form-horizontal">
                    <fieldset>
                        <legend class="text-center header" style="color:green">Delete Movie</legend>
                        <div class="form-group">
                        </div>

                        <div class="form-group">
                            <div class="col-md-10 col-md-offset-1">
                                <label>Name</label>
                                <input type="text" id="Name" name="Name" placeholder="Enter Movie Name For Delete" class="form-control">
                            </div>
                        </div>
                        <br/>
                        <div class="form-group">
                            <div class="col-md-8 text-center">
                                <input type="submit" class="btn-success" value="Delete" />
                            </div>
                        </div>
                    </fieldset>
                </form>-->
            </div>
        </div>
    </div>
</div>


